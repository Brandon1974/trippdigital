[build]
  command = "npm install && echo 'Deploying static site with PayHip products...'"
  functions = "netlify/functions"
  publish = "./"

[functions]
  included_files = ["data/*.json"]

[dev]
  framework = "#custom"
  command = "npm run dev"

# Netlify Functions are bundled from netlify/functions directory

