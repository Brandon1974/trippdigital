[build]
  command = "npm install && echo 'Deploying static site with PayHip products...'"
  functions = "netlify/functions"
  publish = "./"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 404

[dev]
  framework = "#custom"
  command = "npm run dev"

